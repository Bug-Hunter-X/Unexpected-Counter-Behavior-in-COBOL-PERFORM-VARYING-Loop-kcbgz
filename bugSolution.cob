MOVE ZERO TO WS-COUNTER.

PERFORM VARYING WS-SUBSCRIPT FROM 1 BY 1 UNTIL WS-SUBSCRIPT > 10
    ADD 1 TO WS-COUNTER
END-PERFORM.

DISPLAY "Counter value: " WS-COUNTER.

*Corrected version (Ensuring correct initialization and boundary conditions):
MOVE 0 TO WS-COUNTER.
PERFORM VARYING WS-SUBSCRIPT FROM 1 BY 1 UNTIL WS-SUBSCRIPT = 11
   ADD 1 TO WS-COUNTER
END-PERFORM.
DISPLAY "Corrected Counter value: " WS-COUNTER.